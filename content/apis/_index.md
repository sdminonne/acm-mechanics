Goal of this section is to decribe the `ACM` related `kubernetes/openshift` APIs. 
Ideally,if possibile,  for each `API`; we should give the `Kind` a short description and the goal of the `API.


***TODO***: add an example



